{
  "name": "barmagly-fashion-store",
  "version": "1.0.0",
  "private": true,
  "description": "Barmagly Fashion Store - Full-stack e-commerce platform (Kids, Women, Men)",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:setup": "npm run db:migrate --prefix backend && npm run db:seed --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "author": "Barmagly · barmagly.tech",
  "license": "UNLICENSED"
}
